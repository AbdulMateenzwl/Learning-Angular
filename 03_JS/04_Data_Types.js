//  There are Primitive Types (immutable), including
// string, number, bigint, boolean, undefined, null, symbol

let str = "str";
let num = 23
let flag = false


// There are non-Primitive Types (mutable)
// Object, Array, Function, Date, RegExp
let obj = {
    name: "name",
    age: 34
}