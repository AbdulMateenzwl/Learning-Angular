// Function Declaraion
function greet(name) {
    return ("Hello " + name)
}

// Function Expression
const greet = function (name) {
    return ("Hello " + name)
}

// Arrow Functions
const sum = (a, b) => a + b;


// Default Parameters
function add(a = 0, b = 0) {
    return a + b;
}