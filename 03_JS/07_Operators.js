
// Arthmetic
// +, -, *, /, %, **

// Comparison
// ==, ===, != , !==, > , < , <=, >=

// Logical
// &&, ||, !

// Assignment 
// =, +=, -=, *=, /=, %=

// Ternary
let result = age >= 18 ? 'adult' : 'child'